<%= form_for(@cross_selling) do |f| %>
  <% if @cross_selling.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cross_selling.errors.count, "error") %> prohibited this cross_selling from being saved:</h2>

      <ul>
      <% @cross_selling.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nbr_advice_item %><br>
    <%= f.number_field :nbr_advice_item %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
